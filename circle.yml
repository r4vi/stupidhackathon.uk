machine:
  node:
    version: 5.3.0

deployment:
  pre:
    - git config --global user.email "kotecha.ravi+circle@gmail.com"
    - git config --global user.name "Ravi Kotecha"
  production:
    branch: master
    commands:
      - ./node_modules/.bin/gatsby build
      - ./node_modules/.bin/gh-pages -m 'auto commit [ci skip]' -d public
